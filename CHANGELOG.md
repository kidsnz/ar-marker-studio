# Changelog

このファイルの書き方は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)、
バージョンの付け方は [Semantic Versioning](https://semver.org/lang/ja/) に従う。

## [2.3.0] - 2026-08-15

### 変更（判定基準の撤回）
- **「散らばり17%以上で安定」という基準を撤回した。根拠が無かった。**
  実機で答えが分かっている7例を並べたら、**不安定なのは1本だけで、
  それは唯一 3点を割っていたもの**だった:

| マーカー | 最少点 | 最少散らばり | 実機 |
|---|---|---|---|
| emoticons/01（旧） | **2点** | 0.0% | **揺れる** |
| pizzaboy/06 | 3点 | 1.8% | 安定 |
| tweetybird/03 | 4点 | 6.7% | 安定 |
| x/01 | 5点 | 9.5% | 安定 |
| tweetybird/01 | 4点 | 14.1% | 安定 |
| emoticons/01（新） | 5点 | 21.8% | 安定 |
| pizzaboy/07 | 8点 | 22.4% | 安定 |

  安定なものの散らばりは 1.8%〜22.4% とばらばらで、何も予測していない。
  ガイドの「1%/17%/33%」は satoshi の3枚で点数と散らばりが一緒に動いていたため、
  **相関を因果と取り違えていた**。判定はソース由来の3点の下限だけで行い、
  散らばりは参考値として添えるだけにした
- この基準で公開中の25本を診断すると、危ないのは5本だけ（撤回前は17本を
  「揺れる」と誤判定していた）。実機の「他は問題なく動いている」と一致する

## [2.2.0] - 2026-08-15

### 変更
- **採点を「画面上 150〜250px」の一点狙いにした。** 距離の入力欄をなくした。
  実測で3つの使い方が同じ場所（185〜225px）に来ると分かったので、そこに余裕を
  持たせた範囲で採点する。**この的は実寸にも距離にも依らない**ので、
  距離の方を実寸から逆算して「この実寸なら◯m〜◯mで使う」と出す
- **「3パターン全部いける」を一目で出すようにした。** 絵でもステッカーでも
  サイトでも同じファイルで使えるかどうかを、判定の先頭とおすすめの札に出す

### 判定の基準を確定した
既知の5例が**散らばり単独で全部分かれる**ので、点数ではなく散らばりで判定する
（3点の下限だけは別。そこを切ると追従自体が止まる）:

| 例 | 点数 | 散らばり | 実機 |
|---|---|---|---|
| satoshi/03 | 14 | 33.0% | 安定 |
| pizzaboy/07 | 11 | 30.6% | 問題なし |
| satoshi/02 | 9 | 17.0% | ほぼ安定 |
| emoticons/01 | 5 | 11.9% | ふわふわ揺れる |
| satoshi/01 | 3 | 1.0% | 大小に飛ぶ |

点数（12点）を条件に入れると pizzaboy/07 が落ちてしまう。散らばり17%なら
5例すべてが正しく分かれる。「点数より散らばり」というこのツールの原則とも一致する。

実際の出力:
- pizzaboy/07（実機で問題なし）→「3パターン全部いける」
- emoticons/01（実機で揺れる）→「追従はするが、安定はしない。最悪は 2.2m で 8点・11.9%」

## [2.1.1] - 2026-08-15

### 修正
- **画角を camera_para.dat の値に戻した。2.1.0 の「体感に合わせた較正」は誤りだった。**
  実機で測ってもらった結果:
  - iPhone 13 mini を縦持ち、1m 離れて画面の左端から右端まで … **24インチ = 61.0cm**
  - camera_para.dat から出る予測は 60.0cm。**誤差 1.6%**
  つまり元の値で合っていた。ずれていたのは体感の距離の方だった。
  **実測が出たらそちらが正**という当たり前のことを、順番を守らずにやってしまった
- プリセットの距離を実測に合わせた（ステッカーは実測 3〜3.5インチ = 7.6〜8.9cm）

### 分かったこと（2.1.0 の記述を撤回）
**3つの使い方は同じ的だった。** 2.1.0 で「的は2つ」と書いたのは、
構え方ではなく記憶による距離を信じたため。実測を入れると全部そろう:

| 場面 | 距離 | 画面に占める割合 | 画面上の幅 |
|---|---|---|---|
| ステッカー 5.5cm | **7.6〜8.9cm**（実測） | 104〜122% | 188〜221px |
| 絵 72インチ | **2.92m**（実測・115インチ） | **106%** | **191px** |
| ラップトップ画面 12cm | 18〜22cm（未実測） | 92〜112% | 166〜203px |

絵の距離は予測（2.5〜3m）の真ん中に実測が来た。モデルが実機で裏付けられた。

一定なのは**構え方**（画面いっぱい〜少しはみ出すまで寄る）で、実寸は
「どこに立つか」を決めているだけ。**狙う的は 185〜225px の1つ。**

## [2.1.0] - 2026-08-15

### 修正
- **距離の計算に使う画角を実機で較正した。** `camera_para.dat`（ARnft 同梱、
  iPhone 5 の 640×480 用）から出る画角では、72インチの絵が画面いっぱいになるのが
  3.4m という答えになり、実機（2m前後）とまるで合わなかった。あのファイルは
  姿勢計算のための較正であって、いま使っている端末のレンズを表していない。
  確認できた事実「**縦持ちで、距離とほぼ同じ幅が画面の横方向に見える**」で置き換えた
  （2m離れると横に約2m見える）。距離の表示が約1.7倍ずれていたことになる
- 用途プリセットを実測値に直した（ステッカーは実測5.5cm、絵は works ページの
  寸法表記が「高さ×幅」と分かったので幅は大きい方の数字）
- 距離の入力欄の横に「画面の何割を占めるか（画面上何px）」を出すようにした。
  トラッカーが実際に見ているのはそちらなので

### 分かったこと
- **3つの使い方は同じ条件にならない。** 「画面の80〜90%を埋める」は絵でしか
  達成できない。ステッカーで同じ割合にするには6〜7cmまで寄る必要があり、
  iPhone のピントが合わない（約10cmが限界）
  - 絵 72インチ・1.5〜2.2m … 画面上 147〜215px（81〜119%）
  - ステッカー 5.5cm・10〜15cm … 画面上 65〜97px（36〜54%）
  - ラップトップ画面 12cm・25〜40cm … 画面上 53〜85px（29〜47%）
- **3つ全部をカバーするマーカー = 画面上 65〜215px の全域で3点以上を保つマーカー。**
  これが現状の目標
- works ページの寸法表記「A x B」は**高さ×幅**。マーカー画像の縦横比と完全一致で確認
  （pizzaboy/07 = 1.300 ↔ 60x78 → 78/60 = 1.300 など4件が小数3桁まで一致）

## [2.0.0] - 2026-08-15

判定の考え方が変わったので MAJOR を上げる。**「マーカーが画面幅の何割を占めるか」を
やめて、実寸と見る距離で判定する。**

### 変更（互換なし）
- **判定・採点を「実寸 ＋ 見る距離」で行うようにした。** 抽象的な割合をやめた。
  使う人が知っているのは「絵の前に1.5mで立つ」「ステッカーを25cmから読む」の方だから。
  用途のプリセットを3つ置いた（実際の使われ方を本人から聞いて数値化した）
  - 絵 64インチ・1.5〜2m ／ ラップトップ画面 12cm・30〜50cm ／ ステッカー 4.5cm・20〜30cm
- **マーカーが画面より大きいときは、見えている中央だけで点と散らばりを数える。**
  これが今回いちばん大きい修正。大きい絵は必ず画面からはみ出す
  （64インチ幅を1.5mから見ると画布上330px、見えているのは中央55%）。
  隠れている部分の点まで数えていたので、実機と食い違っていた
- 判定の表示を距離ごとの行にした。各行に「画面に何pxで写るか」を出す
- 距離帯の表の色分けを、設定した距離で実際に使われる帯に変えた
- 「暫定」の但し書きを外した（下記のとおり実機で決着したため）

### 判定基準が実機で決着した
実機の報告（絵の前での体感）と突き合わせた結果、
**ガイドの「1% = 飛ぶ / 17% = ほぼ安定 / 33% = 安定」は正しかった。**
測る範囲を間違えていただけだった。中央で測り直すと報告どおりに分かれる:

| マーカー | 中央の点 | 中央の散らばり | 実機 |
|---|---|---|---|
| pizzaboy/07 | 15 | 19.5% | 問題なし |
| edo/01 | 11 | 10.7% | 問題なし |
| **emoticons/01** | 4 | **0.6%** | **ふわふわ揺れる** |

### 分かったこと
- 効いている量は1つだけ … `画面に写る幅(px) = 304.8 × 実寸mm ÷ 距離mm`
  （181px超=はみ出す ／ 82px以上=3点に届く ／ 44px未満=構造的に0点）
- **生成時に宣言する実寸（`-dpi`）は結果を変えない。** 帯の幅も画面上の幅も
  `dpi × 宣言インチ` なので約分で消える。同じマーカーが 4cm のステッカーでも
  1270mm の絵でも同じように動くのはこのため

## [1.3.1] - 2026-08-15

### 修正
- **使わない距離帯を予測しないようにした。** 上の方の距離帯は「マーカーが画面より
  大きく写る」ときにしか使われないのに、画像がいちばん大きい＝いちばん重い側でもある。
  実測（1920×1080）で**予測12.2秒のうち12.0秒（98.4%）がその帯**だった。
  マーカーが画面いっぱいに写るときの見かけdpiの2倍までを計算するようにして、
  **11.9秒 → 0.7秒**（17段中10段のみ計算）。距離表のいちばん近い行より近い側まで
  まかなえているので、画面に出す内容は変わらない。
  飛ばした段数は画面に明記する（生成される `.fset` には全段そのまま入る）
- **Worker にキャッシュ対策が効いていなかった。** `index.html` の `?v=` は
  `<script>` と `<link>` にしか効かず、`new Worker('js/worker.js')` と
  その中の `importScripts('engine.js')` は素の URL で取りに行っていた。
  つまり `?v=` を上げても**ワーカーの中だけ古いまま**残る。
  実際に engine.js の変更がワーカーに届かず、この版の修正が効かなかった。
  app.js が自分の `?v=` を読み取って Worker の URL に引き継ぐようにした
- 追従点の「合計」表示をやめた。もともと「合計は当てにならない」と言っているうえ、
  帯を飛ばすようになって部分的な数になるため

## [1.3.0] - 2026-08-15

### 変更
- **画像を入れたら、品質判定と最適設定の探索まで自動で走るようにした。** ボタンを
  押させる意味が無く、探索は時間がかかるので少しでも早く始めたい
- **探索を `-level=4` だけにした。** `-level` を振る意味が無いと実測で分かったため。
  品質は毎回 level=4 が勝ち、ファイルサイズもほとんど動かない（同じ画像で
  level 4/3/0 が 83/83/83 KB。サイズを決めているのは `.fset3` で `-level` は効かない）。
  1つも勝てない軸のために時間を5倍かけていた。25通り→5通り、
  大きい画像では10通り→2通り（1920×1080 で実測 76.5秒で完了）
- **横持ちの表示を全部やめた**（判定行・距離の表の列・検出のまとめ・文言）
- 距離帯の表から「有効範囲 (dpi)」の列を落とし、距離の表を9行から5行にした
- おすすめを反映したら、その設定での判定をそのまま出すようにした

### 追加
- 探索中の進捗に、いま何倍を生成中か・経過秒・生成器のログ・残り時間の見込みを出した。
  見込みは単純平均にしていない（拡大率が上がると画素数が2乗で増えるため、
  実測2点から 0.6 乗で伸ばす）
- 探索の**中止ボタン**。途中で止めても、そこまでに出た分からおすすめを選ぶ
- 探索中に生成ボタンが押せない理由を画面に出した（同じワーカーを使うため）

### 修正
- 「52cm 〜 52cm で安定」のような幅の無い範囲が壊れて見えていたのを、専用の文言にした

## [1.2.0] - 2026-08-14

### 変更
- **判定の前提を回転版（`ARnft-rot.js`）に合わせた。** 判定に使える画素数を、
  画布の大きさと `object-fit: cover` の可視率から導くようにした。
  これまでの `(180, 240)` は黒帯方式の画布の大きさで、画面に映っていない分を
  引いていない値だった。ガイド第12章の実測（181px / 136px）を再現することを
  `--self-test` で検算する
  - 縦持ち・回転版（本番）… 240 × 320 × 0.753 = **181 × 320**
  - 縦持ち・黒帯（素の ARnft）… 180 × 240 × 0.753 = **136 × 240**
  - 横持ち … 320 × 240 × 0.753 = **320 × 181**
- **見た目を Carnaux/NFT-Marker-Creator に寄せた**（出どころへの敬意として。
  出典はページのフッターと README に明記）
  - 配色を緑＋赤に。できることの緑、まだ押せないものの赤
  - 点線の枠を共通言語にした（画像を置く枠・プレビュー・おすすめの札・ボタン）
  - Generate を大きな点線のパネルにし、押せないあいだは赤い枠のままにした
  - 見出しの `──` などの装飾を落とした
  - 緑の階調だけ変えている。`#42b983` は白地でコントラストが 2.5 しかなく
    読めないため、ライトでは文字に `#27795a`（5.29）を使い、面と枠には元の緑を残す
- 判定の目安（`GOOD=12` / `POOR=5`）について。数え直したところ、
  **旧ツールの 180px は偶然にも回転版の正しい 181px とほぼ一致する**ので、
  目安は失効しておらずそのまま引き継げることが分かった。ただし当時の実機メモに
  未解決の食い違いが残る（黒帯前提で数え直すと satoshi の3枚は 3点/2点/4点になり、
  「ほぼ安定」と記録された02が追従の下限3点を割る）。画面と README に暫定である旨を
  出し、実機で何を確かめれば決着するかをガイド第2章に手順として残した

## [1.1.0] - 2026-08-14

### 追加
- **おすすめの設定**（新セクション3）。拡大率 × `-level` を総当たりで**実際に生成**し、
  性能重視 / バランス / サイズ重視の3案を出す。同じ設定が2役を兼ねるときは1枚にまとめる
- 各案に**選ばれた理由を1行**添える。数字はすべてその画像の実測値で、一般論は書かない
- 各案に**正確なファイルサイズ**（合計と `.fset` / `.fset3` / `.iset` の内訳）
- 各案に**距離ごとの追従点**（マーカーが画面幅の何割を占めるか別）と、3点を切る帯の色分け
- 「この設定にする」で、拡大した画像に差し替えて `-dpi` / `-level` を反映。
  拡大は必ず元画像から行うので、続けて押しても倍率は掛け算にならない
- 生成済みなので、その場で `.fset` / `.fset3` / `.iset` を落とせる
- 試した組み合わせを全部見せる表（黙って切り捨てない）
- `test/pick.js` — 3案の選び方が「判断の軸」どおりかを確かめる検算

### 修正
- **生成器の Module を使い回さないようにした。** 内部に状態が残り、2回目以降の `.fset` が
  1回目のまま返っていた（同じ画像で `-level=4` → 464バイト、続けて `-level=0` でも
  464バイト。作り直せば正しく204バイト）。探索結果が「どの設定でも同じ数字」になって発覚。
  なお Module の生成は 8〜20ms しかかからず、使い回して節約する価値は無かった
- **Emscripten の `Module.then` に `.catch` を繋がないようにした。** `then` は Module 自身を
  返すので `.catch` が無く、毎回 TypeError → ワーカーごと作り直しになり、
  生成が1つおきに失敗していた（25通り中12通りが失敗）
- 散らばりの比較に**許容幅**（5ポイント）を入れた。実測の根拠は 1% / 17% / 33% の3点だけで、
  それより細かい差で選ぶと、1.7% と 1.5% の違いで 291KB 大きい方が選ばれてしまっていた
- ファイルサイズの比較から**格子（`Math.round(kb/10)`）をやめた**。境目が恣意的で、
  104KB と 105KB が「別の大きさ」、100KB と 104KB が「同じ大きさ」になっていた。
  基準を「いちばん小さいもの」に取り、そこから 10KB 以内を同じ大きさとして扱う
- `#bands` / `#dist` 向けの表の CSS が全部の `table` に効いていて、
  新しい表のサイズ列が狭い画面で消えていたのを、対象を限定して直した
- 画像を読み込み直したときに「どこまで離れても効くか」だけ古い内容が残っていたのを直した
- 巨大な画像で拡大するとブラウザが落ちるので、拡大後1600万画素を上限にし、
  試さなかった拡大率は画面に明記するようにした

### 変更
- 公開 README の誤りを2件訂正
  - 「1フレームで試すのは10点」→ **16点**（`setupAR2()` が既定を上書きする）
  - 「合成映像を検出器に流す方式は成立しない」→ **成立する**。悪かったのは当時の実験の
    組み方で、`tools/check_detection.py` が実際にその方式で動いている
- 探索で使う `-dpi` を整数に丸めるのをやめた（画面の入力欄と食い違わないように）

## [1.0.1] - 2026-08-12

### 修正
- 1フレームで試す点数を 10 から 16 に訂正（`setupAR2()` が既定を上書きするため）
- `.fset3` の一覧からダミー段を除いた

## [1.0.0] - 2026-08-11

最初の公開。生成せずに NFT マーカーの品質が分かるツール。

- 距離帯ごとの追従点の予測（本物の生成器のログと突き合わせて検証済み）
- 絵のどこが弱いかを示すヒートマップ3面
- 使える距離の計算
- 検出側（`.fset3`）のキーポイント表示
- 本物の生成器をブラウザ内で走らせる生成機能
- 英語・日本語の切り替え

[2.3.0]: https://github.com/kidsnz/ar-marker-studio/compare/v2.2.0...v2.3.0
[2.2.0]: https://github.com/kidsnz/ar-marker-studio/compare/v2.1.2...v2.2.0
[2.1.1]: https://github.com/kidsnz/ar-marker-studio/compare/v2.1.0...v2.1.1
[2.1.0]: https://github.com/kidsnz/ar-marker-studio/compare/v2.0.0...v2.1.0
[2.0.0]: https://github.com/kidsnz/ar-marker-studio/compare/v1.3.1...v2.0.0
[1.3.1]: https://github.com/kidsnz/ar-marker-studio/compare/v1.3.0...v1.3.1
[1.3.0]: https://github.com/kidsnz/ar-marker-studio/compare/v1.2.0...v1.3.0
[1.2.0]: https://github.com/kidsnz/ar-marker-studio/compare/v1.1.0...v1.2.0
[1.1.0]: https://github.com/kidsnz/ar-marker-studio/compare/v1.0.1...v1.1.0
[1.0.1]: https://github.com/kidsnz/ar-marker-studio/compare/v1.0.0...v1.0.1
[1.0.0]: https://github.com/kidsnz/ar-marker-studio/releases/tag/v1.0.0
